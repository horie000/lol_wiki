# LLM Wiki プロジェクト運用指示

## 目的

この Obsidian Vault は、継続的に知識を蓄積・更新する永続的なナレッジベースである。原典を信頼できる情報源として保持し、生成 Wiki に要約、エンティティ、概念、相互参照、矛盾、統合的な分析を蓄積する。

ユーザーからの明示的な指示は、このファイルおよびリポジトリ内のスキルより優先する。

## 運用言語

- 本リポジトリは日本語で運用する。
- Wiki ページ、索引、ログ、エージェントの作業報告は、ユーザーが別の言語を明示的に指定しない限り日本語で記述する。
- 固有名詞、原典の題名、定着した専門用語は、正確性や検索性のため原語を併記してよい。
- YAML のキー、列挙値、ファイル名、ディレクトリ名、ログの操作種別など、この文書で形式を定めた識別子は英語表記を維持する。
- 引用は原文を保ち、必要に応じて日本語の説明を添える。

## 所有権と境界

- `raw/` はユーザーが管理する不変領域である。内容を読み取ることはできるが、ユーザーが対象と操作を明示しない限り、編集、移動、名前変更、削除を行ってはならない。
- `wiki/` はエージェントが管理する領域である。原典の追加や質問によって知識が増えたとき、ページを作成・更新する。
- `.agents/skills/llm-wiki/` は、取り込み、質問への回答、Lint の再利用可能なワークフローを定義する。
- `.obsidian/` は Vault の設定を保持する。明示的なセットアップまたは設定変更の依頼がある場合だけ変更する。
- `raw/` 外のファイルを根拠資料として扱わない。Wiki ページは原典から派生した案内・統合レイヤーであり、原典そのものの代替ではない。

## ディレクトリ構成

```text
raw/
├── sources/       ユーザーが収集した記事、論文、文字起こし、メモ、データ
└── assets/        原典から参照する画像およびその他のローカル添付ファイル
wiki/
├── index.md       内容別の一覧。Wiki を変更するたびに更新する
├── log.md         時系列の追記専用作業記録
├── overview.md    コレクション全体の案内と現時点の統合的な概要
├── sources/       原典ごとの、根拠に基づく要約
├── entities/      人物、組織、製品、場所などのエンティティ
├── concepts/      概念、手法、テーマ、用語
└── syntheses/     複数の原典にまたがる比較・分析
```

## Markdown の規約

- Obsidian で正しく表示できる UTF-8 Markdown を使用する。
- ファイル名は小文字 ASCII の `kebab-case.md` とする。原典要約は `src-YYYY-MM-DD-short-title.md` とし、判明していれば原典の公開日、不明なら取り込み日を使う。
- Obsidian リンクが曖昧にならないよう、`wiki/` 全体でベースファイル名を一意にする。
- Vault ルートからのパスを指定した Wikilink と日本語の別名を使う。例：`[[wiki/concepts/retrieval-augmented-generation|検索拡張生成（RAG）]]`
- 外部 URL には標準の Markdown リンクを使う。
- ナビゲーションに役立つ箇所へリンクを加える。同じ単語が現れるたびにリンクしてはならない。
- 原典の要約は日本語で記述する。厳密な表現が重要な場合に限り、短い原文引用を使用する。
- 日付は Asia/Tokyo の `YYYY-MM-DD` 形式を使う。不明な任意メタデータは推測せず、省略する。

## ページ要件

`wiki/sources/`、`wiki/entities/`、`wiki/concepts/`、`wiki/syntheses/` に生成するすべてのコンテンツページには、次の YAML フロントマターを付ける。

- `title`
- `type`：`source`、`entity`、`concept`、`synthesis` のいずれか
- `status`：通常は `active`。根拠がある場合だけ `draft` または `superseded` を使う
- `created` と `updated`
- `sources`：原典要約への Wikilink を並べた YAML リスト。原典要約以外のページで必須
- `tags`：少数の安定した語彙を使う YAML リスト

原典要約ページには `source_path` も記録する。判明している場合は `source_url` と `source_date` も記録する。完全な書式は `.agents/skills/llm-wiki/references/page-formats.md` に従う。

すべてのページで、次を満たす。

- 原典に記載された事実、エージェントによる推論、未解決の問いを区別する。
- 有用な場合は、既存の関連ページへ相互リンクを張る。
- `## 出典` セクションを設け、重要な主張を根拠まで追跡できるようにする。
- 意味のある見解の相違は、古い主張を黙って置き換えず保持する。

未解決の矛盾には Obsidian の警告 Callout を使う。

```markdown
> [!warning] 矛盾
> それぞれの主張と原典要約を示し、矛盾の解消に必要な証拠を記述する。
```

## 索引とログ

`wiki/index.md` を正式なページ一覧として維持する。ページ種別ごとに分類してタイトル順に並べ、各項目を Wikilink と一文の説明で記述する。生成した各コンテンツページは、必ず一度だけ掲載する。

`wiki/log.md` は追記専用とする。過去の項目を編集・削除せず、訂正が必要な場合は新しい項目を追記する。見出しは次の解析可能な形式を使う。

```text
## [YYYY-MM-DD] ingest | タイトル
## [YYYY-MM-DD] query | 質問または成果物
## [YYYY-MM-DD] lint | 対象範囲
## [YYYY-MM-DD] maintenance | 変更内容
```

見出しの下には、入力、変更したページ、未解決事項を記載する。

## 運用規則

- 取り込み前に `wiki/index.md` を読み、既存の Wiki ページを検索する。新しい知識は重複ページを作らず、既存知識へ統合する。
- ユーザーが一括処理を求めない限り、原典は一件ずつ処理する。
- 原典要約、影響を受ける知識ページ、`wiki/index.md`、`wiki/log.md` の内容が整合するまで、取り込みは完了していない。
- 質問へ回答するときは、最初に `wiki/index.md`、次に関連する Wiki ページを確認し、検証または不足情報の補完が必要な場合に原典を確認する。
- 再利用可能な回答を保存するときは `wiki/syntheses/` に配置し、索引を更新してログへ追記する。
- Lint では、リンク切れ、孤立ページ、重複したトピック、出典追跡の欠落、古くなった主張、未解決の矛盾、索引・ログの不整合を確認する。安全に修正できる問題は `wiki/` 内だけで修正し、根拠のない主張を追加せず、必要な原典を提案する。
- 不確実性、矛盾、根拠の不足を明記する。記憶や推測で不足を埋めてはならない。
